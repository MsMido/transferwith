# 🚀 초고속 실시간 배차/카풀 시스템 기획 및 명세서 (v1.0)

## 1. 개발 목적 및 기술 스택
* **목표:** 기존 구글 앱 스크립트(GAS)의 딜레이(느린 속도) 해결, 0.1초 미만의 실시간(Real-time) 반응 속도 및 직관적인 터치/드래그 조작 구현
* **프론트엔드:** React (Vite) + Tailwind CSS + dnd-kit (드래그 앤 드롭)
* **백엔드/DB:** Firebase (Firestore) 또는 Supabase (소켓 기반 실시간 데이터 동기화)
* **호스팅:** Vercel 또는 Netlify (무료 글로벌 배포)

---

## 2. UI/UX 레이아웃 설계

### ① 최상단 영역 (네비게이션 & 예외 처리)
* **[좌측 상단] 파티 생성 버튼:** `⊕` (원형 플러스 아이콘). 터치 시 텍스트(시간/목적지) 입력 팝업 출력.
* **[중앙 상단] 탭 전환:** [평일 조작] / [주말 조작] 탭. 선택된 탭에 맞춰 중앙 조작부 배경색이 즉시 전환됨.
* **[우측 상단] 개별 이동 구역:** 
  * 별도 수단으로 이동하는 인원을 위한 상시 고정 구역 (위아래 정렬 박스 형태).
  * 인원 배치 후 [해제] 시 구역 틀은 남고 사람(칩)만 하단 멤버 풀로 복귀.

### ② 중앙 조작부 (스케줄 보드)
* 탭과 동일한 테마 배경색 적용. 가운데 정렬된 **2단 박스** 형태로 파티 목록 나열.
* **박스 구조:** 
  * [첫째 줄] 스케줄 텍스트 (예: 18:00 H마트)
  * [둘째 줄] 멤버 이름표용 빈 공간 (드래그 앤 드롭 영역, 최대 5인)
* **상태 표시 (라벨 & 테두리):** 
  * 박스 우측 상단 텍스트 라벨 및 테두리 색상으로 직관적 표시 (`대기`, `확정`, `SOS(키없음)`).
* **[우측 하단] 강제 확정 버튼:** 2인 이하 상태에서 출발 시 누르는 소형 체크 버튼.
* **전체 해제:** 스케줄 박스 자체를 터치하면 "완료/해제하시겠습니까?" 알림 후 인원 및 키 초기화.

### ③ 최하단 영역 (대기 멤버 풀)
* 중앙부와 완전히 대비되는 배경색 적용. 배차 대기 중인 멤버들의 이름표(칩)가 바닥에 깔려있음.
* **차키(🔑) 시각화 및 양도:**
  * 키 보유자의 이름표 우측에 `🔑` 아이콘 표시.
  * `🔑` 아이콘을 꾹 눌러 다른 사람의 이름표 위로 드래그 앤 드롭 시, 키 소유권 즉시 이전.

---

## 3. 핵심 운용 룰 (Business Logic)

### ① 파티 생성 및 확정 룰
* 누구나 드래그 앤 드롭으로 스케줄 박스에 합류 가능.
* **3인 이상:** 즉시 **[확정]** 상태 (상태 라벨 및 테두리 변경).
* **2인 이하:** **[대기]** 상태. (필요 시 우측 하단 버튼으로 '강제 확정' 가능).
* **노쇼(No-Show) 보호:** 한 번 [확정]된 파티는 인원이 이탈하여 2인 이하가 되어도 [대기]로 강등되지 않고 **[확정] 상태 유지** (빈자리는 공석 처리).

### ② 차키(🔑) 예외 처리 룰
* **노키(No-Key) 파티 보호:** 3인이 모였으나 키 보유자가 0명이면 **[SOS(키없음)]** 상태로 전환. 키 소유자가 합류/키를 양도하는 순간 [확정] 전환.
* **다중 키 파티 유연성:** 1개 파티에 키 보유자가 2명 이상이어도, 인원 조건(3인 이상 등) 충족 시 **키 양도 제약 없이 바로 최종 확정** 가능.
* **잉여 키 유지:** 다중 키 확정 파티에서 남는 잉여 키는 시스템에 강제 반납되지 않고 기존 소유자에게 그대로 귀속(유지)됨.

### ③ 종료 및 초기화 룰
* **원터치 해제:** 스케줄 박스를 누르면 누구나 즉시 운행 종료 가능.
* 실시간으로 전체 접속자에게 해제 알림 송출. 인원은 멤버 풀로 복귀하며, 차키는 기존 소유자에게 그대로 유지됨.

### ④ 주말 특수 룰
* 주말 탭 전환 시, 한 주 주말 동안 순번에 따라 지정된 **'2시간 이하 단기 사용 상시대기 키'** 소유자 현황 출력 및 단기 배차 조작 지원.