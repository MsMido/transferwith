# 실시간 카풀 조율 시스템 v1.0 - 프론트엔드 개발 정리

## 1. 파일 모듈화 구조
다중 AI 협업 및 유지보수를 위해 전체 코드를 역할별로 분리했습니다.

* **`src/App.jsx`**: 전체 레이아웃 뼈대, Firebase 실시간 데이터 상태 관리, 드래그 앤 드롭 통합 규칙(`DndContext`)을 통제하는 중앙 컨트롤러.
* **`src/components/DraggableMember.jsx`**: 드래그 가능한 개별 네임택 UI 및 `useDraggable` 훅 담당. (사람 이동과 키 이동 핸들 분리)
* **`src/components/DroppableSchedule.jsx`**: 스케줄 카드 UI 및 인원을 배치하는 `useDroppable` 구역 담당.
* **`src/components/WaitingPool.jsx`**: 화면 하단에 고정되는 대기열 UI 및 드롭 영역 담당.

## 2. 추가된 핵심 기능
* **드래그 앤 드롭 최적화**: `@dnd-kit/core`를 활용하며, 하단 고정 영역 인식을 위해 충돌 감지 방식을 `pointerWithin`으로 적용했습니다.
* **운전자(키) 권한 독립 이동**: 사람 이름표와 별개로 '키' 뱃지만 독립적으로 드래그하여 다른 인원에게 넘겨줄 수 있습니다. 한 사람이 여러 대의 차량(키)을 관리할 수 있도록 `keyCount` 속성을 적용했습니다.
* **Firebase 실시간 DB 연동**: `onSnapshot`을 통해 DB 변경사항이 즉각 화면에 반영되며, 모든 조작(드래그 앤 드롭, 스케줄 생성/완료)은 `setDoc`을 통해 Firestore에 실시간으로 업데이트됩니다. 여러 기기에서 동시 접속 시 새로고침 없이 상태가 동기화됩니다.
* **새 스케줄 동적 생성**: 모달창을 통해 신규 이동 경로를 입력받고, 고유 ID와 함께 DB에 즉시 등록하는 기능이 추가되었습니다.

## 3. 코드 작성 및 관리 규칙
* **컴포넌트 분리 원칙**: UI와 기능이 독립적인 단위(네임택, 대기열, 스케줄 박스)는 반드시 개별 컴포넌트 파일로 분리하여 코드 가독성을 높입니다.
* **상태 관리 단방향 플로우**: 모든 상태 변화는 Firebase DB를 거쳐 `onSnapshot` 리스너를 통해 로컬 상태로 반영되도록 일원화했습니다.
* **스타일링**: Tailwind CSS를 활용하여 직관적이고 반응형에 유연한 디자인을 구성하며, 상태에 따른 조건부 클래스 렌더링(예: `isOver` 상태에 따른 하이라이트)을 적극 활용합니다.
* **데이터 불변성 유지**: 상태 배열이나 객체를 업데이트할 때는 `filter`, `map`, 전개 연산자(`...`)를 사용하여 기존 데이터를 직접 수정하지 않고 새로운 배열/객체를 반환합니다.
